1. make it so when learning mode is enabled the request analyzer runs on requests and 
suggests rules in enable proxy tab

when you feel like it use the api instead of the js 

2. start getting more advanced: content types, headers used strict/dynamic, etc